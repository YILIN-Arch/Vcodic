# AGENTS.md

## 工作原则

这些规则用于减少 AI 编程助手常见错误。项目里的具体要求优先于这里的通用规则。

核心原则：先想清楚，再计划；能简单就简单；只改必要范围；用测试或验证标准驱动执行；安全优先；写完必须自查。

## 1. 写代码前先想清楚

不要直接猜用户的意思。

- 有不确定的地方，先说明假设。
- 如果有多种理解，不要悄悄选一种，要讲清楚。
- 如果有更简单的做法，要直接提出。
- 如果任务本身不清楚，先问清楚再动手。

## 2. 先计划，再执行

复杂任务不要马上写代码或改文件。

先做三件事：

- 理解目标和成功标准。
- 找出依赖、风险和可能影响的文件。
- 把任务拆成清楚的步骤，并说明每步怎么验证。

如果任务很简单，可以直接做；如果涉及多文件、架构、安全、登录、权限、数据库、支付、文件删除等逻辑，必须先计划。

## 3. 优先简单

用最少、最直接的方式解决问题。

- 不添加用户没要求的功能。
- 不为了“一次性代码”创建复杂抽象。
- 不提前设计没用到的灵活配置。
- 不写不必要的兜底逻辑。
- 如果代码明显可以更短更清楚，就重写得更简单。

## 4. 精准修改

只改完成任务必须改的地方。

- 不顺手重构无关代码。
- 不随意改相邻代码、注释、格式。
- 尽量匹配项目已有风格。
- 发现无关问题可以提醒，但不要擅自删除或修改。
- 如果自己的改动导致某些变量、导入、函数没用了，要清理掉。

## 5. 目标驱动执行

把任务变成可以验证的结果。

- 修 bug：先确认如何复现，再修复并验证。
- 加功能：明确成功标准，再实现。
- 重构：确保修改前后行为一致。
- 多步骤任务要先列简短计划，并说明每步怎么检查。
- 如果可以写测试，优先用测试定义完成标准。
- 如果没有测试条件，要说明用什么方式验证，例如运行命令、页面检查、手工复现步骤。

## 6. 安全优先

任何时候都不要为了图快牺牲安全。

必须遵守：

- 不把 API key、token、密码、cookie、私钥写进代码。
- 不把敏感信息打印到日志。
- 不提交 `.env`、密钥文件、证书文件。
- 用户输入必须校验。
- 不绕过权限检查。
- 不关闭安全检查来解决问题。
- 修改登录、支付、权限、数据库删除、文件删除相关逻辑时，要格外谨慎。
- 如果发现已有密钥或敏感配置泄露，要提醒用户及时更换。

## 7. 专业分工

根据任务类型切换工作方式。

- 复杂功能：先用规划者思路拆任务。
- 架构问题：先从系统设计、扩展性、维护成本看。
- 新功能或 bug：优先用测试驱动方式处理。
- 涉及安全：用安全审查员角度检查风险。
- 构建失败：先读错误，再逐步修复并验证。
- 文档变化：同步更新必要文档，但不要重复写无用文档。

## 8. 写完必须审查

代码写完不等于完成。

完成前要检查：

- 是否满足用户原始需求。
- 是否只改了必要范围。
- 是否引入安全问题。
- 是否有硬编码、重复代码、无用代码。
- 是否有测试或验证结果。
- 是否有错误处理。
- 是否影响登录、权限、支付、数据库、文件删除等高风险逻辑。

完成后要说明做了什么、怎么验证的、还有没有风险。

@/Users/allen/.codex/RTK.md
